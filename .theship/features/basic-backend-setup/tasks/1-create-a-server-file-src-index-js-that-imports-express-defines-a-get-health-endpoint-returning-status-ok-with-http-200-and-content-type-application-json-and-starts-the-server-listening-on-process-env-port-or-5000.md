# Task: Create a server file (src/index.js) that imports Express, defines a GET /health endpoint returning {"status":"Ok"} with HTTP 200 and Content-Type application/json, and starts the server listening on process.env.PORT or 5000.
**ID**: cmqwikqux000604i3y65maqmd
**Status**: TODO

## Description
Create a server file (src/index.js) that imports Express, defines a GET /health endpoint returning {"status":"Ok"} with HTTP 200 and Content-Type application/json, and starts the server listening on process.env.PORT or 5000.

## Instructions
1. Review the requirements in ../01_PRD.md.
2. Complete the code updates.
3. Mark this task as done by checking it in ../tasks.md.
